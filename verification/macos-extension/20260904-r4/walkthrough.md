# r4 真实点击走查存证（NVR-*）

- 日期：2026-09-04 12:37–12:41（本机时钟）
- 被测对象：`apps/desktop/macos/dist/SmartBLE-macOS.app`（ad-hoc 签名，bundle id `com.smartble.desktop`）
- 启动方式：LaunchServices `open`（非直接执行二进制）
- 驱动方式：宿主 computer-use AX 通道（语义 AXPress / AXIncrement 元素级真实事件；非程序化 `.perform()` 调用）
- 限制：宿主屏幕录制权限被拒 → 无截图；自绘视图（设备卡/MenuRowButton）无 AX press 动作且无光栅帧可坐标点击 → 相关单项 NOT_RUN

## 逐项记录

| ID | 操作（真实 AX 事件） | 观察结果 |
| --- | --- | --- |
| NVR-01 | `open` 启动 bundle → 观察 AX 树 | 窗口 "BLE Toolkit+"（740×652）；四 Tab（扫描/已连接/广播/关于）齐全；P001 内容完整（蓝牙就绪/待开始扫描/开始扫描/筛选/空态文案） |
| NVR-02 | （平台事实登记）启动授权路径 | 从已授权终端 `open` 启动：TCC 按责任进程归因继承终端蓝牙授权，**未弹 bundle 自身授权框**，蓝牙直接就绪。Finder/Dock 启动才走 `NSBluetoothAlwaysUsageDescription` 弹框路径（未演练 → 该正向弹框路径 NOT_RUN） |
| NVR-03 | 点击「开始扫描」→ 等 7.5s | 真实扫描发生并 5s 自动停止：状态「扫描完成 · 发现 5 台」、计数 chip=5、5 张真实设备卡（未命名 BLE·0D07/-50dBm、·4C9D/-77、·DF6C/-84、midea×2/-86、-92），每卡带「连接」按钮 |
| NVR-04a | 点击「筛选」→ 面板展开 | 预设四档（强[-40]/较好[-60]/一般[-70]/弱[-85]）、阈值滑杆、名称前缀、隐藏无名、重置过滤全部渲染 |
| NVR-04b | 点击「较好 [-60]」 | 计数 5→1、滑杆 -100→-60、仅剩 -50dBm 一台（真实过滤生效） |
| NVR-04c | 滑杆 AXIncrement | 阈值 -60 → -57（真实步进） |
| NVR-04d | 切换「隐藏无名」开关 | 开关 0→1；唯一设备为未命名 → 列表切空态「当前没有匹配设备 / 调整筛选条件试试」 |
| NVR-04e | 点击「重置过滤」 | 阈值回 -100、开关回 0、5 台设备全部恢复 |
| NVR-05 | 点击 Tab「广播」 | P008 完整：平台徽标「Desktop · macOS」、原生层说明、四字段、ADV 预算 21/31 字节分项（10+4+7）、通信日志含走查真实历史 |
| NVR-06 | 点击「检查支持」 | 状态「未就绪→已就绪」；日志新增「蓝牙从机已就绪 · 支持 peripheral 广播」 |
| NVR-07a | 点击「开始广播」 | 真实广播启动：状态「广播中」、表单按规范禁用（字段标注"广播中禁用"）、按钮翻转为「停止广播」、检查支持禁用、日志「开始广播 · name=SmartBLE uuid=FFE0 platform=Desktop·macOS」+「广播已启动」 |
| NVR-07b | 点击「停止广播」 | 状态「已停止」、表单恢复可编辑、日志「广播已停止」（约 7s 真实广播窗口） |
| NVR-08 | 点击 Tab「关于」 | P009 完整：品牌卡（v0.1.0-spike · preview/零后端·零本地持久化）、更多小程序两行、应用信息（当前环境 Desktop · macOS 26.5 / 设备型号 / 构建投影）、平台状态矩阵（微信小程序 VERIFIED、App·iOS UNSUPPORTED、桌面端 REFERENCE 全 NOT_RELEASED）、生态能力矩阵（广播监听 ⚠️ / 广播发送 ❌【待验证】冲突 C2 / 多设备 ⚠️） |
| NVR-09 | 点击 LightBLE「前往」→「下载小程序码」→「✕」 | promo sheet 真实弹出（落地页承接说明 + 示意小程序码 + 打开落地页/下载小程序码）；下载按钮点击派发成功，toast 为 2.2s 瞬态胶囊未被 AX 快照捕获（r3 UIS-10 已程序化验证拦截文案）；✕ 关闭 sheet |
| NVR-10 | 点击 Tab「已连接」 | P007 空态：SESSIONS / 通用调试会话 chip /「还没有连接中的设备」+ 去扫描 |
| NVR-11a | 点击窗口关闭按钮 | 退出确认 modal 弹出：「桌面端为常驻运行。确认退出？（10_platform §4 生命周期：常驻，退出确认）」+ 继续使用/退出 |
| NVR-11b | 点击「继续使用」 | modal 关闭、窗口留存（仍在 P007） |
| NVR-11c | 再次关闭 → 点击「退出」 | 进程真实终止（pgrep 无 SmartBLE-macOS 进程） |

## NOT_RUN / 限制登记

1. **设备卡点击（F004 广播详情 sheet）**：卡片为自绘容器视图，AX 树仅暴露内部文本与「连接」按钮，无卡片级 press 动作；宿主屏幕录制被拒 → 无光栅帧可做坐标点击。r3 冒烟 UIS-05 已程序化覆盖该 sheet 内容。
2. **P009 操作系统行菜单 / 应用菜单行**：MenuRowButton 自绘（内容为子视图），AX 无 press 动作；r3 UIS-10 已程序化覆盖（actionId 定位）。
3. **视觉截图**：宿主 Screen Recording denied（截图工具返回明确拒绝）。视觉证据沿用 r3 `--snap-pages` cacheDisplay 9 页 PNG。
4. **bundle 自身蓝牙授权弹框正向路径**：见 NVR-02——终端责任进程归因绕过了该路径；需 Finder/Dock 冷启动观察，未在本轮演练。

## 结论

AX 事件级真实交互链（扫描→过滤→广播启停→页面切换→弹层→退出确认双路径）全部按预期工作，与 r3 程序化冒烟结果一致；无新增 UI 缺陷。真实点击走查维度：**PASS（含 4 项诚实 NOT_RUN 登记）**。
