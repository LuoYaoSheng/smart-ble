# 修复与重构路线图

优先级：**正确 → 可用 → 闭环 → 一致 → 可维护 → 优化**。

## Phase 1：P1 正确性（2026-08-29 状态）

| 项 | 状态 |
|---|---|
| P007 App 广播前置失败可见 | **Done**（`validateAppBroadcastStart` + `reportBroadcastError`） |
| P001 已配置 Smart HID 历史入口 | **Done**（查看/移除；不展示 token/Wi-Fi） |
| OTA 不以写完为成功；等 `{status:success}` | **Done**（客户端 + 单测）；固件/真机仍待 |

## Phase 2：核心业务闭环（2026-08-29 状态）

| 项 | 状态 |
|---|---|
| P005 常驻连接中/空服务/失败/重试 | **Done**（`resolveServicePanelState` + 手动重试） |
| P002 扫码失败反馈、配网等待取消、离页确认 | **Done**（`describeScanCodeFailure` + `cancelProvisionWait` + `onBackPress`） |
| P006 空态「去扫描」；批量断开列出失败项 | **Done**（`summarizeDisconnectAllResults`） |

## Phase 3：跨页面状态与生命周期（2026-08-29 状态）

| 项 | 状态 |
|---|---|
| knownDevices 过期（90 天）/同 ID 取最新 / 上限 20 | **Done**（`normalizeKnownDevices` + `pruneKnownDevices`） |
| 设备日志内存上限 | **Done**（每设备 200、最多跟踪 40、全局 500） |
| 紧凑设备路由（避免大 JSON） | **Done**（`device-route-context` + stash） |
| 真机验 P001↔P005/P006、配网、广播 hide/show | **待真机** |

## Phase 4：统一重复业务能力（2026-08-29 状态）

| 项 | 状态 |
|---|---|
| `useDeviceSession` | **Done**（P005 已接入） |
| `useBroadcastSession` | **Done**（可复用 composable；页面平台分支仍内联） |
| 设备路由上下文避免大 JSON | **Done**（随 Phase 3） |
| 平台权限/广播 capability adapter | 后续 |

## Phase 5：高价值公共组件（2026-08-29 状态）

| 项 | 状态 |
|---|---|
| `OperationState` | **Done**（P002/P005 service-panel 已接入） |
| `empty-state` action | **Done**（`actionLabel`；P006 使用） |
| 可配置 `LogPanel` | **Done**（title/caption/clear/compact/card；P007 已接入） |

## Phase 6：UI / 交互 / Token（2026-08-29 状态）

| 项 | 状态 |
|---|---|
| 错误/成功/状态行 token | **Done**（`.ble-error-box` / `.ble-success-box` / `.ble-status-line`） |
| Surface / line / shadow / input token | **Done**（`--ble-gradient-surface` / `--ble-line-*` / `--ble-shadow-*` / `--ble-input-bg`） |
| 高频页面/组件收敛到 token | **Done**（about/hid/index/broadcast + device-card/service-panel/dialogs/navbar/filter/log-panel） |
| 进行中按钮 `.ble-btn--busy` | **Done**（与 disabled 同视觉；OTA/写值/配网重连/诊断刷新） |
| 工具类 `.ble-surface-item` / `.ble-input` / hairline | **Done** |

## Phase 7：完整回归（待真机）

1. `./scripts/verify-uniapp.sh` 作 PR 门禁（自动化已覆盖 Phase 1–6 契约）。
2. HBuilderX/微信自动化覆盖四 Tab。
3. Android/iOS/微信真机矩阵：见 `docs/verification/uniapp-real-device-checklist.md`（含 Phase 1–5 增补项）。
4. OTA：有 `{status:success}` 的固件须等到确认；无 status 的固件须诚实超时失败。
