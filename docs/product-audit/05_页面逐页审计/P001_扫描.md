# P001 扫描

页面路径：`pages/index/index`；类型：Tab。复核：2026-08-29。

## A. 页面目的
发现附近 BLE 设备；管理本机已配置 Smart HID 历史；进入通用 GATT 或 Smart HID 配网。

## B. 页面信息结构
自定义导航 → 扫描摘要/错误 → **已配置 Smart HID 历史区（条件）** → 筛选 + 设备列表/空态 → 广播弹窗。

## C. 页面内容合理性
历史区文案标明「不代表在线」，正确。卡片点击看广播、按钮连接，职责可分但 affordance 仍偏弱。

## D. 用户操作（操作清单）

| 操作ID | UI入口 | 用户操作 | 触发代码 | 预期结果 | 实际结果 | 后续 |
|---|---|---|---|---|---|---|
| O1 | scan-summary 主按钮 | 开始/停止扫描 | `toggleScan` → `useBleScan.toggle/start` | 列表更新或停止 | PASS（逻辑+单测）；真机待验 | 留在本页 |
| O2 | 重试 | 扫描失败后重试 | `@retry` → `startScan` | 再授权并扫描 | PASS 同 O1 | 本页 |
| O3 | 筛选 | 展开/改筛选 | `showFilters` + `filter-panel` | 列表过滤 | PASS | 本页 |
| O4 | 设备卡 | 查看广播 | `showAdvertisingData` | 弹窗 | PASS | 弹窗 |
| O5 | 连接按钮 | 通用连接 | `connectDevice` → `prepareConnect` → `navigateTo` P005 | 进入详情 | PASS | P005 |
| O6 | Profile 按钮 | Smart HID 配网 | `openProfileDevice` → hidStore → P002 | 进入配网 | PASS | P002 |
| O7 | 历史「查看」 | 打开已配置设备 | `openKnownHidDevice` → `buildHidDetailUrl` | 进 P003 | **PASS（已落地）** | P003 |
| O8 | 历史「移除」 | 删本机记录 | `removeKnownHidDevice` → modal → `removeKnownDevice` | 列表更新 | PASS | 本页 |
| O9 | 弹窗复制 | 复制广播 | `copyAdvData` → clipboard | toast | PASS | 弹窗 |

## E. 页面跳转
→ P005 / P002 / P003；Tab 切换。

## F. 页面状态
待扫/扫描中/完成/错误/无结果/无匹配/有历史；缺权限引导中、连接按钮 busy。

## G–I. 数据 / API / Store
`useBleScan` + `ble`；`hid.knownDevices`；Bluetooth adapter / scan permission / clipboard。

## J. 完整运行链路（关键操作）

### O1 开始扫描
```text
UI → toggleScan → useBleScan.start
→ requestBleScanPermission → PASS/微信引导
→ bleStore.startScan → scan session → uni.startBluetoothDevicesDiscovery
→ 单一发现回调 → 标准化合并 → computed 列表
判定：链路 PASS；真机 PARTIAL
```

### O7 历史查看
```text
UI → openKnownHidDevice → setCurrentDevice → navigateTo hid/detail
→ P003 读 knownDevices / currentDevice
判定：PASS（原 R-05 已消除）
```

## K–M. 首次 / 返回 / 异常
不自动扫；从 P005 返回保留内存列表；微信区分蓝牙/定位拒绝。

## N. 假完成
无。

## O. 功能遗漏
历史无直达重配/诊断；无清空扫描结果。

## P–Q. 交互 / 信息
卡片点击含义不显著（P3）。

## R. 第一断点
无静态断点；真机授权回流待验。

## S. 后续潜在断点
弱名称匹配 Smart HID 误导 → P002 二次确认可回退。

## T–V. 复用与职责
组件/composable 复用良好；页面编排轻量。

## W. 优化建议
卡片显式「查看广播」；历史项可选「重配」快捷入口；连接 busy。

## X. 最终结论
产品合理性 A；UI 85%；核心可用 80%（含历史）；链路 85%；异常 75%；复用 90%。
