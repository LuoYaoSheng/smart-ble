# 01 页面总表

> 基线 `bf5d17e`。来源：`pages.json` + 全项目 `uni.navigateTo/redirectTo/switchTab/navigateBack/navigateToMiniProgram` 全量扫描 + 页面文件比对。

## 总表

| ID | 页面 | 路径 | 页面类型 | 入口 | 可跳往 | 核心作用 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P001 | 设备（扫描） | `pages/index/index` | **Tab 页**（启动默认页，custom navbar） | 启动 / tabBar / 分享落地 / 各流程返回 | P006（通用连接）、P002（配网）、P005（历史入口卡） | 全 App 唯一扫描触发点；发现设备、区分 Smart HID、管理连接会话与配置历史入口 | ✅ 已审计（见 P001 文档） |
| P002 | 配置 Smart HID | `pages/hid/add` | 普通页（三段式向导：连接/填写配置/查看状态） | P001 设备卡「Smart HID 配网」；P003「重新配置」（带 READY 停广播确认弹窗） | P003（配网成功 redirectTo）、P004（恢复：mqtt_invalid→诊断）、P001（switchTab 完成）、系统返回 | Smart HID 配网主流程：GATT 连接→身份确认→扫码取 token→下发 Wi-Fi/Hub 配置→跟踪状态机 | 待审计 |
| P003 | Smart HID 设备 | `pages/hid/detail` | 普通页 | P002 成功后 redirectTo；P005 点历史记录 | P002（重新配置）、P004（运行诊断）、P001（switchTab「高级 BLE 调试」） | 查看已配网设备资料（deviceId/固件/协议/上次配置），提供管理与诊断入口 | 待审计 |
| P004 | 诊断 | `pages/hid/diagnostics` | 普通页（工具页） | P003「运行诊断」；P002 恢复动作「进入诊断」 | **无出边**（仅系统返回） | BLE/Wi-Fi/Hub/MQTT/Ready 五项链路诊断与重连 | 待审计 |
| P005 | Smart HID 历史 | `pages/hid/history` | 普通页 | P001 入口卡（仅 `knownDevices.length > 0` 时显示） | P003（点记录） | 本地已配网设备历史（≤20 条，非敏感元信息），查看详情/移除记录 | 待审计 |
| P006 | 设备详情（通用调试） | `pages/device/detail` | 普通页（工具页） | P001 设备卡「连接」（通用与 Profile 设备共用）；P001 已连接 tab 卡片 | **无出边**（仅系统返回） | 通用 GATT 调试器：服务/特征浏览、读写、notify、OTA、通信日志、自动重连 | 待审计 |
| P007 | BLE 广播 | `pages/broadcast/index` | **Tab 页** | tabBar | 无页面跳转（含分享） | 本机模拟 BLE 外设广播（微信 peripheral 模式 + App 端 LysBlePeripheral 原生插件双路径） | 待审计 |
| P008 | 关于 | `pages/about/index` | **Tab 页** | tabBar | P009（版本记录）、外部小程序 ×2（navigateToMiniProgram） | 品牌信息、功能介绍、相关应用导流、版本入口、分享 | 待审计 |
| P009 | 版本记录 | `pages/about/version` | 普通页 | P008「版本记录」 | 无出边 | 版本历史展示（至 v1.0.5）、分享 | 待审计 |

## 页面类型统计

Tab 页 3 · 普通页 6 · 分包 0 · WebView 0 · 登录/授权页 0 · 弹窗型业务页 0（弹窗均为组件：advertisement-dialog / write-dialog / ota-dialog）

## 判定明细

### 未注册页面

无。`pages/**` 下 9 个 `.vue` 页面文件与 `pages.json` 9 条注册一一对应。

### 疑似废弃页面 / 死代码（页面级无废弃，代码级残留 2 处）

| 项 | 位置 | 证据 | 判定 |
| --- | --- | --- | --- |
| `hasConfiguredDevice` getter | `store/hid.js:54` | 全项目零读取方（P001 用 `knownDevices.length > 0` 自行判断） | 死代码，建议删除 |
| `smartDevices` ref | `store/hid.js:43` | 零写入方；但 `use-smart-hid-provisioning.js:103` 读取它做设备解析 fallback，且注释声明"二期 Profile 扫描预留" | 文档化死代码，保留但须有人真正接二期 |

### 孤岛页面（可进入，无合理退出或后续流程）

| 页面 | 分析 |
| --- | --- |
| P004 诊断 | 出边为 0。作为工具页可接受（系统返回兜底），但"诊断出问题之后没有下一步动作"是产品断点，详细判定见 P004 审计 |
| P006 通用设备详情 | 出边为 0。调试工具页属性决定，可接受 |

### 重复页面

| 组 | 分析 |
| --- | --- |
| P003（Smart HID 设备）vs P006（设备详情） | 同为"设备详情"语义的两套体系：Profile 体系（Smart HID 业务态）与通用调试体系（GATT 原语）。FIX-07 已把标题区分为「Smart HID 设备」vs「设备详情」。是否合并为"一个详情页 + 模式切换"属设计决策，留待业务矩阵阶段评估 |
