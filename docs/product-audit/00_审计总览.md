# SmartBLE UniApp 综合审计总览

审计日期：2026-08-28（基线）／复核刷新：2026-08-29；范围：`apps/uniapp` 及其直接依赖的 `core/ble-core`、`core/protocols`。

## 执行前分析（2026-08-29 刷新）

- 页面：9 个已注册；4 个 Tab（扫描、已连接、广播、关于）；5 个二级页；无 `subPackages`；无未注册页面文件。
- 核心模块：通用扫描与 GATT 调试、连接会话、BLE 广播、Smart HID 配网/诊断、关于与版本。
- 公共资产：14 个 Vue 组件、2 个 Pinia Store、2 个 composable、服务层含 `ble-runtime` / `provisioning` / `smart-hid` / `broadcast` 等。
- 主链路：`页面 → composable/store → ble-runtime/provisioning/transport → uni/wx/plus API 或原生插件 → 状态/日志/UI`。
- 孤岛/入口：P003 已可通过 P001「已配置 Smart HID」历史区进入（WIP 已落地）；仍无独立「历史管理」页，列表动作仅查看/移除。
- 高风险（仍成立）：GATT 空服务 UI、配网等待取消弱、OTA **依赖设备端 JSON status**（客户端已改；多数固件未实现时会超时失败——诚实失败，不再假成功）。
- 已降级：广播 App 静默早退（已加校验与错误上报）；Smart HID 历史完全无入口。

## 与提示词符合度

| 维度 | 状态 |
|---|---|
| 文档集 00–16 + 逐页 A–X | 齐全 |
| 操作清单七列表 + 操作级 PASS/FAIL | **2026-08-29 起在 P001/P002/P005/P007 加深**；其余页仍为摘要级 |
| 用户场景 / 设备生命周期 | 见刷新后的 `04` |
| 禁止边审边大改 | 审计基线保留；Phase 1 代码修复单独进行并回写结论 |

## 证据与验证边界

本地可跑 `./scripts/verify-uniapp.sh`（单测 + 静态关卡）。真机 BLE/微信/原生插件行为仍标 **待真机验证**。

## 总体结论

主路径分层清晰。Phase 1 三项中：**广播失败反馈**与**历史入口**已在代码落地；**OTA** 客户端改为「必须收到 `{status:success}` 才成功」，未确认则超时/失败——消除假完成，但若设备固件不发 status，用户将看到失败而非误报成功。下一步优先 Phase 2（GATT 空态、配网取消）与真机矩阵。

## 问题统计（刷新后）

| 严重度 | 数量 | 含义 |
|---|---:|---|
| P0 | 0 | 无静态确认的全员阻断 |
| P1 | 1 | OTA 设备端协议未实现时仅能诚实失败；真机闭环未验 |
| P2 | 4 | GATT 空态、配网取消、跨页状态、扫码失败反馈 |
| P3 | 6 | 信息架构与交互 |
| P4 | 4 | 架构抽取 |
| P5 | 4 | 规范漂移 |

详见 `06`–`16`。
