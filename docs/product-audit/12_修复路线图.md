# 12 · 修复路线图

> 汇总 06-11 全部发现，按「先验证 → 先阻断 → 先闭环 → 再打磨」排出四个 Phase + 一组跨端 backlog。
> 每条含：问题编号、修复方案、涉及文件、验收标准。**验收总则（沿用 §35）：不是代码有没有，而是用户能不能真的完成这件事——每项修复必须配真机走查截图/录屏存档。**

### 基线演进备注（2026-08-29）

并行 Cursor 会话在远程落了「Phase 1–6 修复」（`276d9dc`+`2a8c554`+合并 `5e2b40c`），与本路线图部分撞车。核对结果：
- **其已覆盖**：R2 假按钮（携 deviceId 跳通用详情）、R3 扫码分流（`scan-code-feedback.js` 纯函数）、R7 角标回写（`syncScannedDeviceConnectionStatus` + 会话注册表自动断开同步，比原方案更完整）、R12 恢复词汇统一（workflow 单一来源）。
- **其未覆盖、由提交「补齐 R4/R5/R6+计数口径」完成**：R4 扫描到点播报（含 0 设备终态——组件状态字在 0 设备时仍误显「待开始」）、R5 向导 configure 阶段断开即时可见、R6 诊断后动作出口、R7 已连接计数纳入 Smart HID 会话。
- 该远程基线曾存在**语法断裂**（`smart-hid/index.js` 的 `disconnect` 函数声明被误删，文件无法解析），已在本提交一并修复；全部 JS 已过 `node --check`。

---

## 0. 问题总账（输入映射）

| 严重度 | 数量 | 条目（问题编号） |
|---|---|---|
| P1（待验证） | 1 | 适配器双模冲突（09-M01 / 08 §3） |
| P1（用户点名） | 1 | P001 文案重复（07-D01 / P001-I03） |
| P2 | 7 | P001-I01、P001-I02、P001-I04、P002-I01、P002-I02、P003-I01、P004-I01 |
| P3 | 16 | P001-I05~I08、P002-I03~I05、P003-I02、P004-I02、P005-I01、P006-I01、P006-I05、P007-I02、P007-I03、P008-I01 |
| P4 | 15 | P003-I03/I04、P004-I03/I04、P005-I02、P006-I02~I04、P007-I04/I05、P008-I02/I03、P009-I01/I02 |
| P5 | 1 | smartDevices 二期预留（记录不动） |

---

## Phase 0 · 真机验证（阻断一切后续定级）｜预估 0.5 天

| 编号 | 验证项 | 方法 | 产出 |
|---|---|---|---|
| V1 | 适配器双模冲突是否复现 | 真机矩阵：①先广播→切 Tab 扫描；②先扫描→开广播；③广播中发起连接；④广播后杀进程再进。各 10 轮，记录 errCode 与行为 | 复现 → 09-M01 定级 P1，进 Phase 1；不复现 → 降级 P3 架构债，仍建议 Phase 3 收口 |

> Phase 0 是整条路线图唯一的「先做」依赖：Phase 1 的规模取决于它。

## Phase 1 · 阻断级（P1/P2 中破坏信任与功能者）｜预估 2~4 天（视 V1）

| 编号 | 修复项 | 方案 | 涉及文件 | 验收标准 |
|---|---|---|---|---|
| R1 | 双模冲突（若 V1 复现） | runtime 增加 mode 维度：`ensureAdapterReady(mode)` 记录当前模式；central 操作前发现 mode=peripheral 则先 `closeBluetoothAdapter` 再重开；广播页改走 runtime，删除直调 | `services/ble-runtime/index.js`、`pages/broadcast/index.vue:214/:554` | 广播↔扫描往返 10 轮零误导性报错；全应用仍只有 runtime 一处 open 调用点 |
| R2 | P003 假按钮（FC-01） | ✅ 已修复（并行 Cursor Phase1-6）：携 deviceId 经 `buildGenericDeviceDetailUrl` 跳通用详情 | `pages/hid/detail.vue` | 待真机过单 R2-1~4（见 `13_Phase0_真机验证用例.md`） |
| R3 | 相机权限死路径（FC-02） | ✅ 已修复（并行 Cursor Phase1-6）：`describeScanCodeFailure` 三路分流（取消 toast / 权限弹窗 / 失败弹窗） | `services/smart-hid/scan-code-feedback.js` + composable | 待真机过单 R3-1~5（见 13 号文档） |

## Phase 2 · 核心闭环补全（5 处「沉默事件」+ 计数口径）｜预估 2~3 天

| 编号 | 修复项 | 方案 | 涉及文件 | 验收标准 |
|---|---|---|---|---|
| R4 | 扫描完成反馈（P001-I01） | ✅ 已修复：composable `start` 在 `reason==='timeout'` 时播报「扫描完成 · 发现 N 台 / 未发现设备」（补上组件状态字在 0 设备时误显「待开始」的缺口）；主动停止不播报 | `composables/use-ble-scan.js` | 待真机过单 R4-1~4（见 13 号文档） |
| R5 | 配置阶段断开可见（P002-I02） | ✅ 已修复：service 暴露 `onSessionDisconnect`；向导 configure 阶段断开即时置 `connectionLost`（toast + 「已断开」徽标 + 重连按钮，表单内容保留）。status 阶段经 `statusWaiters.failAll` 本就立即失败 | `services/smart-hid/index.js`、`composables/use-smart-hid-provisioning.js`、`pages/hid/add.vue` | 待真机过单 R5-1~4（见 13 号文档） |
| R6 | 诊断后动作出口（P004-I01） | ✅ 已修复：新增「返回设备详情」「重新配网」（页面栈感知导航，复用 `hid-navigation.js` URL 构造器；重新配网带 READY 引导弹窗） | `pages/hid/diagnostics.vue` | 待真机过单 R6-1~4（见 13 号文档） |
| R7 | 连接计数与回写（P001-I04/I07） | ✅ 已修复（两半合并）：角标回写由并行 Cursor 的 `syncScannedDeviceConnectionStatus`+会话注册表完成；计数口径由本提交补齐——hid store `sessionOnline`（service 写入），首页计数=通用连接+Smart HID 会话，已连接页空态说明配网进行中 | `store/hid.js`、`store/ble.js`、`services/smart-hid/index.js`、`pages/index/index.vue`、`pages/connected/index.vue` | 待真机过单 R7-1~4（见 13 号文档） |

## Phase 3 · 一致性与文案（用户点名项在此）｜预估 2 天

| 编号 | 修复项 | 方案 | 验收标准 |
|---|---|---|---|
| R8 | P001 文案去重（**用户点名，P1**） | 「附近设备」「已连接」各保留一处，区块语义重划 | 用户复查通过 |
| R9 | 导航栏状态准确（P001-I02） | 导航栏蓝牙状态改为派生自 runtime adapter 状态，与真实一致 | 开/关蓝牙真机对照无误报 |
| R10 | 产品自述更新（P008-I01） | PRODUCT_FEATURES 加「Smart HID 配网」；summary 直说核心场景 | 关于页出现配网能力表述 |
| R11 | 装饰性状态清理（P001-I05/I06、P003-I04） | 删静态「连接稳定」或改 RSSI 派生；协议 chip 去 success 色；更新定位权限文案 | 页面无「假状态」元素 |
| R12 | 恢复词汇合并（P002-I03） | `workflow.smartHidRecoveryAction` 与 `recoveryFor` 合并为单一来源，另一处删除或改为引用 | grep 仅一处定义恢复动作枚举 |
| R13 | 广播/OTA/写入反馈（P007-I02/I03、P006-I01/I05） | 截断发生时提示实际保留内容；`chooseMessageFile` fail 加 toast；INPUT 特征值写入前警示 | 三处操作失败/边界均有可见反馈 |
| R14 | （若 V1 不复现）广播调用仍收口 runtime | 同 R1 方案，作为架构债偿还 | 同 R1 验收 |

## Phase 4 · 打磨与清债（P4 全量 + 死代码）｜预估 1~2 天

| 编号 | 修复项 | 条目 |
|---|---|---|
| R15 | 信息展示 | hardware 元数据展示（P003-I03）、设备编号统一展示一套（S01/P003-I02）、lastError 时间戳（P004-I04）、诊断标签统一（P004-I03）、空态图语义（P005-I01）、envVersion 按环境取值（P008-I03）、分享落地页指向首页（P009-I01） |
| R16 | 边界反馈 | knownDevices 溢出提示（P005-I02）、`stopAdvertising` fail 处理（P007-I04）、广播状态单点显示（P007-I05）、「导出日志」命名对齐行为（P006-I03） |
| R17 | 死代码清理 | P006 dead imports（:57）、legacy `options.device` 分支、版本页硬编码样式并入 token（P009-I02） |

## 跨端 backlog（不在小程序本期内，来自 04/S5 与 11-方向四）

| 项 | 动机 | 前置 |
|---|---|---|
| 固件物理 RECOVERY 触发 | 重配网 5 分钟等待，小程序端已无优化空间 | 固件仓立项 |
| 协议 V2 · Wi-Fi 扫描特征值 | 消除 SSID 手输错误（最高频配网失败源） | 固件+小程序同步 |
| 配网 V2 流程提案 | 用户已有产品方向直觉 | 先做方案对比再立项 |

---

## 路线图总览

```
Phase 0 真机验证 ──┬─ 复现 → Phase 1 (R1+R2+R3) → Phase 2 → Phase 3 → Phase 4
（0.5 天）         └─ 不复现 → Phase 1 (R2+R3) → Phase 2 → Phase 3(+R14) → Phase 4
总预估：5~11 个工作日（不含跨端 backlog）
```

**收尾判据**：06-10 五份清单中的 P1/P2 全清零、P3 清零或降级记录、08 假完成清单仅剩「诚实降级」一栏；04 六个场景全部 ✅；03 闭环率从 77% 提升至 90%+（剩余 ⚠️ 均为跨端依赖项）。
