# Smart BLE 公开落地页规范

## 目标

落地页是开源项目的统一入口，帮助访问者在一分钟内回答：

1. BLE Toolkit+ 是什么？（多平台多框架的开源 BLE 工具家族）
2. 当前哪个版本真正可用？
3. 能做哪些 BLE 操作？
4. 如何用 ESP32 从零复现？
5. 去哪里体验、下载、读文档和参与贡献？

落地页不展示内部审计与未验证完成度；多平台实现家族按统一产品规范如实呈现，不把在研实现线伪装成已发布，也不使用「多端大一统」「全平台统一下载」类定位。

## 信息架构

落地页当前实现按以下分区（与 `docs/index.md` 保持同步）：

1. **Hero**：产品名 BLE Toolkit+；定位一句话覆盖四支柱（调试 · 学习 · 多平台多框架对照 · 硬件联动）；主行动「微信扫码使用」（真实小程序码 + 扫码文字等价），次行动文档 / GitHub。
2. **获取（#get）**：小程序码卡第一，其余平台给真实源码构建路径；无产物不放假下载。
3. **项目定位（#why）**：三痛点（工具分散 / 资料零散 / 实现难对比）对三提供（可直接用的调试工具 / 多框架对照样例 / 软硬件一体学习联动），附适用人群与教程、架构指南入口。
4. **当前能力（#features）**：扫描与智能状态过滤、连接与动态发现、读写订阅（UTF-8 / HEX 封解包）、多设备并行与事件防串扰、外设模式广播（128 位 UUID 强校验）、OTA 分包下发验证；每卡带诚实状态注记。
5. **工作流**：发现设备 → 检查广播 → 连接 → GATT 读写/Notify → 日志 → 广播/OTA → ESP32 验证。
6. **平台状态（#platforms）**：多平台多框架家族八卡（微信小程序 / Android / iOS·macOS / Flutter / 桌面多路线 / H5 / Web / ESP32），每卡列技术栈与角色、链接对应教程；脚注诚实声明发布状态。
7. **Smart HID 与 Profile 生态（#profile）**：第一方 Smart HID Profile（配网/诊断/历史）、SHA-256 契约锁对齐 Smart-HID-Workspace、开放 Profile 扩展流程。
8. **界面一览（#gallery）**：真机实拍截图，标注来源平台与版本。
9. **开源与贡献**：仓库、Issue、贡献指南、License、状态页；Security 入口缺失时不提供假链接。

落地页不展示内部审计与未验证完成度；多平台实现家族按统一产品规范如实呈现，不把在研实现线伪装成已发布，也不使用「多端大一统」「全平台统一下载」类定位。

### 附：核心闭环

用一条简洁流程展示：

```text
发现设备 → 检查广播 → 连接 → GATT 读写/Notify → 日志 → 广播/OTA → ESP32 验证
```

### 附：当前能力细则

展示经过测试的能力卡：扫描、筛选、连接、多设备、Read/Write/Notify、广播、OTA、Profile、Smart HID。每张卡链接到对应功能编号或教程。

### 附：平台状态细则

落地页以「多平台多框架，一个工具家族」呈现实现家族全景，每卡链接对应构建教程；发布状态用词与 `08_RELEASE_GATES.md`/状态页一致，不弱化：

| 实现线 | 框架 / 技术 | 落地页定位 |
|---|---|---|
| 微信小程序 | UniApp（Vue）一线 | 扫码即用 · 开发预览（主推入口，真实小程序码 + 扫码文字等价） |
| Android | UniApp 一线 + 原生 Kotlin（apps/android） | 客户端主线 · 开发预览 |
| iOS / macOS | SwiftUI + CoreBluetooth，共享 SmartHidCore（apps/ios） | 原生线 · 源码可构建 |
| Flutter | Android / macOS（apps/flutter） | 跨框架 · 源码可构建 |
| 桌面 | Tauri / Electron / macOS Native（apps/desktop） | 桌面线 · 源码可构建 |
| H5 | UniApp 一线 | 降级体验 · 无真实 BLE（明确标注不支持） |
| Web | 本站（VitePress）+ HTML 交互原型 | 原型 · 可交互 |
| ESP32 固件 | ESP-IDF（hardware/） | 硬件夹具 · 开发中 |

脚注必须诚实声明：各实现线当前均未发布正式产物；微信小程序可扫码体验，其余平台可从源码构建；能力与发布状态以状态页与 `05_PLATFORM_MATRIX.md` 为准。

### 附：ESP32 参考硬件

- 开发板和接线图
- 一键构建/刷写命令
- 三组服务和 UUID
- LED、权限特征值、Notify、OTA 能力
- Android/微信测试矩阵
- 下载固件或从源码构建

### 附：交互原型

嵌入或链接 HTML 原型，说明可切换微信、Android、iOS、H5，查看正常、空、失败、断线和不支持状态。

### 附：快速开始

分为三条 5 分钟路径：

- 我只想体验微信小程序
- 我要安装 Android App
- 我要刷 ESP32 并完成第一次联调

### 附：测试与可信度

公开展示最新 release 的：commit、App/小程序版本、ESP32 固件版本、通过的平台、已知限制和证据入口。

### 附：开源与贡献

- MIT 许可证
- 仓库与 Issue
- 架构和产品契约
- 新增 Profile 指南
- 测试设备/平台贡献方式
- 行为准则和安全报告入口

## 实现约束

- 使用现有 VitePress `docs/index.md` 和品牌资产演进，不新建第二套官网技术栈。
- 页面数据优先来自产品契约/构建时元数据，避免手工复制平台状态。
- 响应式覆盖手机和桌面；核心内容无 JavaScript 也能阅读。
- 图片有 alt，导航键盘可达，对比度和焦点可见。
- 下载、二维码、外部链接必须在每次发布后自动或人工验证。

## 不展示的内容

- 内部 P0/P1 审计过程和重复旧文档列表
- 未通过真机却标记“完整支持”的平台
- 不可下载的占位按钮
- 历史产品名混用
- 私有 Smart HID token、内部 ControlHub 地址或测试账号

## 验收

- 首屏能识别产品、当前主版本和三个主要入口。
- 从落地页最多两次点击到达微信、Android、ESP32、原型和贡献指南。
- 平台状态与 `05_PLATFORM_MATRIX.md` 一致。
- 能力声明与 `08_RELEASE_GATES.md` 的证据一致。
- 手机、桌面、暗色/亮色和无障碍基本检查通过。
