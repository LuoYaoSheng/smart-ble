> [!NOTE]
> English translation is currently work-in-progress. Displaying the original Chinese text for now.

# 鎶€鏈繁搴﹁В鏋愶細濡備綍璁捐涓€涓法骞冲彴 BLE 鎶借薄灞?

## 鏍囬
```
濡備綍璁捐涓€涓法骞冲彴 BLE 鎶借薄灞傦紵Smart BLE 鏋舵瀯璁捐鎻
```

---

## 姝ｆ枃鍐呭

```markdown
# 濡備綍璁捐涓€涓法骞冲彴 BLE 鎶借薄灞傦紵Smart BLE 鏋舵瀯璁捐鎻

## 鍓嶈█

鍦ㄥ紑鍙?Smart BLE 鐨勮繃绋嬩腑锛屾渶澶х殑鎸戞垬涔嬩竴灏辨槸锛?*濡備綍璁?8+ 绉嶅钩鍙板叡浜竴濂楃粺涓€鐨?BLE 浠ｇ爜閫昏緫锛?*

uni-app 鐢ㄥ井淇″皬绋嬪簭 API锛孎lutter 鐢?flutter_blue_plus锛屾闈㈢鐢?noble/btleplug...姣忎釜骞冲彴鐨?API 閮戒笉涓€鏍凤紝鎬庝箞鍔烇紵

绛旀灏辨槸锛?*璁捐涓€涓法骞冲彴 BLE 鎶借薄灞?*銆?

---

## 涓€銆佷负浠€涔堥渶瑕佹娊璞″眰锛?

### 1.1 鐥涚偣

涓嶅悓骞冲彴鐨?BLE API 宸紓宸ㄥぇ锛?

```typescript
// 寰俊灏忕▼搴?
wx.startBluetoothDevicesDiscovery({
  success: (res) => { }
})

// flutter_blue_plus
await flutterBluePlus.startScan(timeout: Duration(seconds: 4));

// noble (Node.js)
noble.startScanningAsync([], false);
```

濡傛灉娌℃湁鎶借薄灞傦紝涓氬姟閫昏緫浠ｇ爜浼氬彉寰楁贩涔憋細

```typescript
// 绯熺硶鐨勪唬鐮?- 鍒板閮芥槸骞冲彴鍒ゆ柇
if (platform === 'wechat') {
  wx.startBluetoothDevicesDiscovery()
} else if (platform === 'flutter') {
  await flutterBluePlus.startScan()
} else if (platform === 'electron') {
  noble.startScanning()
}
// ... 鍒板閮芥槸杩欑鍒ゆ柇
```

### 1.2 鎶借薄灞傜殑濂藉

- 鉁?涓氬姟閫昏緫缁熶竴锛屽彧闇€鍐欎竴娆?
- 鉁?鏂板骞冲彴鍙渶瀹炵幇鎺ュ彛
- 鉁?浠ｇ爜鏇存槗缁存姢鍜屾祴璇?
- 鉁?闄嶄綆瀛︿範鎴愭湰

---

## 浜屻€佹娊璞″眰璁捐

### 2.1 鏍稿績鎺ュ彛瀹氫箟

```typescript
/**
 * BLE 閫傞厤鍣ㄧ粺涓€鎺ュ彛
 * 鎵€鏈夊钩鍙板疄鐜伴兘蹇呴』閬靛惊杩欎釜鎺ュ彛
 */
interface IBLEAdapter {
  // ========== 鍒濆鍖?==========
  initialize(): Promise<void>
  getState(): Promise<AdapterState>

  // ========== 鎵弿 ==========
  startScan(options?: ScanOptions): Promise<void>
  stopScan(): Promise<void>
  onDeviceFound(callback: (device: ScanResult) => void): void

  // ========== 杩炴帴 ==========
  connect(deviceId: string): Promise<void>
  disconnect(deviceId: string): Promise<void>
  onConnectionStateChanged(callback: (state: ConnectionState) => void): void

  // ========== 鏈嶅姟鍙戠幇 ==========
  discoverServices(deviceId: string): Promise<BLEService[]>

  // ========== 璇诲啓鎿嶄綔 ==========
  readCharacteristic(serviceId: string, charId: string): Promise<DataBuffer>
  writeCharacteristic(
    serviceId: string,
    charId: string,
    data: DataBuffer,
    writeType?: WriteType
  ): Promise<void>

  // ========== 閫氱煡 ==========
  setNotification(
    serviceId: string,
    charId: string,
    enable: boolean
  ): Promise<void>
  onCharacteristicChanged(callback: (data: CharacteristicData) => void): void

  // ========== 骞挎挱锛堝璁炬ā寮忥級==========
  startAdvertising(options: AdvertisingOptions): Promise<void>
  stopAdvertising(): Promise<void>

  // ========== 娓呯悊 ==========
  cleanup(): Promise<void>
}
```

### 2.2 绫诲瀷瀹氫箟

```typescript
// 閫傞厤鍣ㄧ姸鎬?
enum AdapterState {
  Uninitialized = 'uninitialized',
  PoweredOff = 'poweredOff',
  PoweredOn = 'poweredOn',
  Unauthorized = 'unauthorized',
  Unsupported = 'unsupported'
}

// 鎵弿閫夐」
interface ScanOptions {
  serviceUUIDs?: string[]     // 鍙壂鎻忕壒瀹氭湇鍔?
  allowDuplicates?: boolean    // 鏄惁鍏佽閲嶅璁惧
  timeout?: number             // 瓒呮椂鏃堕棿锛堟绉掞級
}

// 鎵弿缁撴灉
interface ScanResult {
  id: string                   // 璁惧 ID
  name?: string                // 璁惧鍚嶇О
  rssi: number                 // 淇″彿寮哄害
  serviceUUIDs: string[]       // 鏈嶅姟 UUID 鍒楄〃
  manufacturerData?: DataBuffer// 鍘傚晢鏁版嵁
  advertisData?: AdvertisementData
}

// 杩炴帴鐘舵€?
interface ConnectionState {
  deviceId: string
  status: 'connecting' | 'connected' | 'disconnected'
  error?: Error
}

// 鏈嶅姟
interface BLEService {
  uuid: string
  isPrimary: boolean
  characteristics: BLECharacteristic[]
}

// 鐗瑰緛鍊?
interface BLECharacteristic {
  uuid: string
  properties: {
    read: boolean
    write: boolean
    notify: boolean
    indicate: boolean
  }
  value?: DataBuffer
}

// 鍐欏叆绫诲瀷
enum WriteType {
  WithResponse = 'withResponse',
  WithoutResponse = 'withoutResponse'
}

// 骞挎挱閫夐」
interface AdvertisingOptions {
  name?: string
  serviceUUIDs?: string[]
  manufacturerData?: DataBuffer
  connectable?: boolean
}
```

---

## 涓夈€佸钩鍙板疄鐜?

### 3.1 uni-app 瀹炵幇

```typescript
class UniAppBLEAdapter implements IBLEAdapter {
  async initialize(): Promise<void> {
    return new Promise((resolve, reject) => {
      wx.openBluetoothAdapter({
        success: () => resolve(),
        fail: (err) => reject(err)
      })
    })
  }

  async startScan(options?: ScanOptions): Promise<void> {
    return new Promise((resolve, reject) => {
      wx.startBluetoothDevicesDiscovery({
        services: options?.serviceUUIDs,
        allowDuplicatesKey: options?.allowDuplicates ?? false,
        success: () => {
          // 鐩戝惉鎵弿缁撴灉
          wx.onBluetoothDeviceFound((res) => {
            res.devices.forEach(device => {
              this.onDeviceFoundCallback?.(this.parseDevice(device))
            })
          })
          resolve()
        },
        fail: (err) => reject(err)
      })
    })
  }

  async connect(deviceId: string): Promise<void> {
    return new Promise((resolve, reject) => {
      wx.createBLEConnection({
        deviceId,
        success: () => resolve(),
        fail: (err) => reject(err)
      })
    })
  }

  // ... 鍏朵粬鏂规硶瀹炵幇
}
```

### 3.2 Flutter 瀹炵幇

```dart
class FlutterBLEAdapter implements IBLEAdapter {
  final FlutterBluePlus _flutterBlue = FlutterBluePlus.instance;

  @override
  Future<void> initialize() async {
    // 妫€鏌ヨ摑鐗欑姸鎬?
    await _flutterBlue.isOn;
  }

  @override
  Future<void> startScan([ScanOptions? options]) async {
    await _flutterBlue.startScan(
      timeout: Duration(seconds: options?.timeout ?? 5),
      withServices: options?.serviceUUIDs
        ?.map((uuid) => Guid(uuid))
        .toList(),
    );

    // 鐩戝惉鎵弿缁撴灉
    _flutterBlue.scanResults.listen((results) {
      for (ScanResult r in results) {
        onDeviceFoundCallback?.call(_parseDevice(r));
      }
    });
  }

  @override
  Future<void> connect(String deviceId) async {
    await _flutterBlue.connect(deviceId);
  }

  // ... 鍏朵粬鏂规硶瀹炵幇
}
```

### 3.3 Electron (Noble) 瀹炵幇

```typescript
class NobleBLEAdapter implements IBLEAdapter {
  private noble = require('@abandonware/noble');

  async initialize(): Promise<void> {
    return new Promise((resolve, reject) => {
      this.noble.on('stateChange', (state) => {
        if (state === 'poweredOn') resolve()
      })
    })
  }

  async startScan(options?: ScanOptions): Promise<void> {
    this.noble.on('discover', (peripheral) => {
      this.onDeviceFoundCallback?.(this.parseDevice(peripheral))
    })
    this.noble.startScanning(
      options?.serviceUUIDs ?? [],
      options?.allowDuplicates ?? false
    )
  }

  async connect(deviceId: string): Promise<void> {
    const peripheral = await this.findPeripheral(deviceId)
    return new Promise((resolve, reject) => {
      peripheral.connect((error) => {
        error ? reject(error) : resolve()
      })
    })
  }

  // ... 鍏朵粬鏂规硶瀹炵幇
}
```

---

## 鍥涖€佷娇鐢ㄦ娊璞″眰

### 4.1 涓氬姟閫昏緫缁熶竴

鏈変簡鎶借薄灞傦紝涓氬姟閫昏緫鍙渶瑕佸啓涓€娆★細

```typescript
class BLEManager {
  private adapter: IBLEAdapter

  constructor(adapter: IBLEAdapter) {
    this.adapter = adapter
  }

  // 鎵弿骞惰繛鎺ョ涓€涓尮閰嶇殑璁惧
  async scanAndConnect(serviceUUID: string): Promise<void> {
    // 1. 鍒濆鍖?
    await this.adapter.initialize()

    // 2. 鎵弿
    await this.adapter.startScan({ serviceUUIDs: [serviceUUID] })

    // 3. 绛夊緟鍙戠幇璁惧
    return new Promise((resolve) => {
      this.adapter.onDeviceFound((device) => {
        if (device.serviceUUIDs.includes(serviceUUID)) {
          // 4. 杩炴帴
          this.adapter.connect(device.id).then(() => {
            this.adapter.stopScan()
            resolve()
          })
        }
      })
    })
  }

  // 璇诲彇鐗瑰緛鍊?
  async readCharacteristic(serviceId: string, charId: string) {
    return await this.adapter.readCharacteristic(serviceId, charId)
  }

  // 鍐欏叆鐗瑰緛鍊?
  async writeCharacteristic(
    serviceId: string,
    charId: string,
    data: DataBuffer
  ) {
    await this.adapter.writeCharacteristic(serviceId, charId, data)
  }
}
```

### 4.2 鍒囨崲骞冲彴

鍒囨崲骞冲彴鍙渶瑕佹洿鎹㈤€傞厤鍣細

```typescript
// 寰俊灏忕▼搴?
const wxAdapter = new UniAppBLEAdapter()
const bleManager = new BLEManager(wxAdapter)

// Flutter
const flutterAdapter = new FlutterBLEAdapter()
const bleManager = new BLEManager(flutterAdapter)

// Electron
const nobleAdapter = new NobleBLEAdapter()
const bleManager = new BLEManager(nobleAdapter)
```

---

## 浜斻€佽璁″師鍒?

### 5.1 鎺ュ彛鏈€灏忓寲

鍙畾涔夋牳蹇冩柟娉曪紝淇濇寔鎺ュ彛绠€娲侊細

```typescript
// 鉁?濂界殑璁捐 - 鏍稿績鏂规硶
interface IBLEAdapter {
  connect(deviceId: string): Promise<void>
  disconnect(deviceId: string): Promise<void>
}

// 鉂?涓嶅ソ鐨勮璁?- 杩囦簬鍏蜂綋
interface IBLEAdapter {
  connectWithTimeout(deviceId: string, timeout: number): Promise<void>
  connectWithRetry(deviceId: string, retry: number): Promise<void>
  connectAndDiscover(deviceId: string): Promise<void>
}
```

### 5.2 寮傛浼樺厛

鎵€鏈夋搷浣滈兘杩斿洖 Promise锛岄€傚簲鐜颁唬寮傛缂栫▼锛?

```typescript
// 鉁?濂界殑璁捐
async connect(deviceId: string): Promise<void>

// 鉂?涓嶅ソ鐨勮璁?
connect(deviceId: string, callback: (error: Error) => void): void
```

### 5.3 閿欒缁熶竴

缁熶竴鐨勯敊璇鐞嗘満鍒讹細

```typescript
class BLEError extends Error {
  constructor(
    public code: BLEErrorCode,
    message: string,
    public platformData?: any
  ) {
    super(message)
  }
}

enum BLEErrorCode {
  ADAPTER_NOT_INITIALIZED = 'ADAPTER_NOT_INITIALIZED',
  DEVICE_NOT_FOUND = 'DEVICE_NOT_FOUND',
  CONNECTION_FAILED = 'CONNECTION_FAILED',
  SERVICE_NOT_FOUND = 'SERVICE_NOT_FOUND',
  OPERATION_TIMEOUT = 'OPERATION_TIMEOUT'
}
```

---

## 鍏€佹€荤粨

涓€涓ソ鐨勮法骞冲彴鎶借薄灞傚簲璇ワ細

1. **鎺ュ彛绠€娲?* - 鍙畾涔夋牳蹇冩柟娉?
2. **绫诲瀷瀹夊叏** - 瀹屾暣鐨勭被鍨嬪畾涔?
3. **寮傛浼樺厛** - 閫傚簲鐜颁唬缂栫▼妯″紡
4. **鏄撲簬鎵╁睍** - 鏂板骞冲彴鎴愭湰浣?
5. **閿欒鍙嬪ソ** - 缁熶竴鐨勯敊璇鐞?

---

## 椤圭洰鍦板潃

- **GitHub**: https://github.com/luoyaosheng/smart-ble
- **Gitee**: https://gitee.com/luoyaosheng/lys-smart-ble/tree/refactor%2Fmulti-platform/

瀹屾暣浠ｇ爜瀹炵幇娆㈣繋鏌ョ湅椤圭洰婧愮爜锛?

---

*鏍囩锛?鏋舵瀯璁捐 #璺ㄥ钩鍙?#BLE #钃濈墮 #TypeScript #Flutter #uni-app*
```

