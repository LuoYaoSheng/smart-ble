# HTML V0 原型验收报告（阶段五）

> 对应重构 SOP：旧 5 阶段模板·阶段五；新 8 阶段 SOP（《旧 App AI 重构 SOP》）Phase 3「HTML V0 原型（旧产品镜像）」之验收确认。
> 验收角色：**测试负责人**（只读验收，本轮未修改任何代码）。
> 验收日期：2026-09-02。

## 0. 验收信息

| 项 | 内容 |
|---|---|
| 验收对象 | `prototype/v0-old/app-prototype.html`（单文件，1231 行；手机框架 + 评审面板，全模拟数据，无后端） |
| 对比基线 ① | `docs/01_reverse/REVERSE_ANALYSIS.md`（阶段一，历史记录不回写） |
| 对比基线 ② | `docs/02_product/PRD.md`（阶段二，含 2026-09-02 变更记录） |
| 对比基线 ③ | `docs/03_flow/PAGE_SPEC.md`（阶段三） |
| 对比基线 ④ | `docs/09_test/COVERAGE_CHECKLIST.md`（阶段四前置产出） |
| 验收口径 | **2026-09-02 变更后**：PAGE004、F023、R19 已移除/作废，不在验收范围；`REVERSE_ANALYSIS.md` 与原型的差异按 prd.md 变更记录解释 |
| 验收方法 | 静态注册表核对（PAGES/SCEN/ACTIONS 与三文档逐条比对）+ 浏览器功能断言（Playwright，历史四轮 + 当日复核） |
| 复现方式 | `cd prototype/v0-old && python3 -m http.server 8931` → 打开 `http://127.0.0.1:8931/app-prototype.html`（评审面板可直达 9 页与 43 个场景） |

## 1. 页面覆盖 —— 通过（在册 10/10）

PAGES 注册 9 个可渲染页面 + 1 页移除，与 prd.md §6 页面需求（变更后）一致：

| 页面 | 原型渲染器 | 入口 | 结论 |
|---|---|---|---|
| PAGE001 扫描首页 | renderP001 | tabBar「扫描」（默认页） | ✅ |
| PAGE002 配网向导 | renderP002 | P001 SHID 卡「配置 Smart HID」/ P003「重新配置」/ P005「重新配网」 | ✅ |
| PAGE003 SHID 设备详情 | renderP003 | P002 成功 redirectTo / P005「返回设备详情」（栈感知） | ✅ |
| PAGE004 SHID 历史 | —（已移除） | — | 按变更口径不验收 |
| PAGE005 SHID 诊断 | renderP005 | P003「运行诊断」/ P002 错误恢复分流 | ✅ |
| PAGE006 通用设备详情（含 OTA 子流程） | renderP006（ota-dialog 覆盖层） | P001 普通卡「连接」/ P007 点卡 / P003「高级 BLE 调试」 | ✅（返回入口见缺失列表 D1） |
| PAGE007 已连接设备 | renderP007 | tabBar「已连接」 | ✅ |
| PAGE008 BLE 广播 | renderP008 | tabBar「广播」 | ✅（见 O1） |
| PAGE009 关于 | renderP009 | tabBar「关于」 | ✅ |
| PAGE010 版本记录 | renderP010 | P009「版本记录」菜单 | ✅ |

tabBar 为 4 页（P001/P007/P008/P009），与旧 App pages.json tabBar 配置一致（来源：reverse-analysis §3）。

## 2. 功能覆盖 —— 通过（在册 29/29）

`COVERAGE_CHECKLIST.md` 功能覆盖表（F001–F030，F023 已移除）逐条核对成立，本轮抽验与全量点击断言未见缺失。代表性映射：

- F001–F005 扫描链：`toggle-scan` / `toggle-filter` / `open-adv`（广播数据弹窗含「本轮平台 API 未提供此字段」标注）/ 无名兜底「未命名 BLE · ID 后四位」——历史断言 R2、当日复核均通过。
- F006–F013 GATT 工作台：连接流（connecting→ready/error/empty）、服务树折叠+SIG 中文名、读取/写入/监听、六色日志、清空/导出、被动断线自动重连——历史断言 R7–R10 通过。
- F014–F017 广播：微信/Android/Web 三平台场景、冲突与权限、31 字节预算超限阻断——历史断言 R11–R13 通过。
- F018–F022、F024 配网与诊断：STRONG 徽章、三步骤条、扫码四分支、分帧写入四行进度至 READY、wifi_failed/pairing_expired/controlhub_unreachable 恢复分流、五项诊断六态、错误码详情——历史断言 R1、R3–R6 通过。
- F025 OTA：选包→manifest 校验（sha256 不符失败场景）→分块传输→提交→验证→成功 2s 自动关闭/取消——历史断言 R14/R17 通过。
- F026–F030：脱敏演示条目、P009/P010 版本元数据、推广卡两态、分享平台分支、国际化「现状标注」（如实呈现，未脑补）。

## 3. 操作覆盖 —— 通过（2 处形态差异，见缺失列表）

- 原型 `ACTIONS` 注册 **52 个动作键**，模板标记 `data-act` 去重后 **58 处引用**（含 tab/jump/scen/back 等通用动作）。
- page-spec 每页「按钮与热区」清单逐条对照：均有对应实现（含 write-dialog、ota-dialog、modal 内按钮）；`@click.stop` 防穿透类热区（如设备卡「查看广播数据」vs 卡内按钮）以 `data-stop` 容器模拟。
- 弹窗类操作（写入、OTA、扫码、移除确认、断开确认）均含确认/取消双分支——「用户取消」检查项通过。

## 4. 状态覆盖 —— 通过（五态 5/5）

SCEN 注册 **43 个场景**，分布：P001×5、P002×8、P003×3、P005×4、P006×8、P007×4、P008×10、P009×1（P010 为静态列表页，自带三列表空态，无需场景切换）。与 coverage-checklist 状态覆盖表一致：

| 模板五态 | 原型覆盖（抽验来源） |
|---|---|
| 加载 | P001 扫描中 / P002 连接中·配网中 / P005 检测中 / P006 connecting·OTA 传输 / P008 启动中 |
| 成功 | P002 READY / P005 全正常 / P006 读值·OTA 成功 / P008 广播中 |
| 失败 | P001 扫描失败横幅+重试 / P002 连接失败+8 类错误码 / P005 连接失败 / P006 服务 error·OTA sha256 不符 / P008 启动失败 |
| 空数据 | P001 未扫描·筛选无匹配 / P006 服务空 / P007 两种空态 / P008 日志空 / P010 三列表空 |
| 异常 | P002 断线续填·扫码权限 / P003 记录不存在 / P005 未连接 / P008 权限缺失·蓝牙未开·连接冲突·开发者工具·Web 不支持 |

**六项特别检查（空/加载/错误/权限/网络异常/取消）**：逐页核对见 page-spec 各页「异常处理」小节，原型均有对应场景或显式 N/A。其中「网络异常」对本产品整体为 N/A——旧 App 零 HTTP 后端、无登录（reverse-analysis §2/§8），无网络层可模拟；权限类统一映射为蓝牙/定位/扫码权限场景。

## 5. 异常覆盖 —— 通过

- 配网 8 类错误码恢复动作分流（回表单/回配对/去诊断/可重试）——R6 断言通过。
- 扫描失败横幅（含自动重试 3 次文案）+ 手动重试——R16 断言通过。
- OTA manifest sha256 校验失败、传输中断取消——R17 断言通过。
- 广播 31 字节超限红字阻止启动——R13 断言通过。
- Web 平台不支持、微信开发者工具限制、活动连接冲突、Android 蓝牙未开/权限缺失——R11–R12 断言通过。
- 写入空值/非法 HEX 拦截、读取 3s 超时日志——R9–R10 断言通过。
- P003 记录不存在弹窗返回、P005 未连接提示、P007 全部断开部分失败清单弹窗——R15/R18 断言通过。

## 6. 浏览器断言记录

| 轮次 | 日期 | 范围 | 结果 |
|---|---|---|---|
| 第一轮 | 2026-09-02 | 配网全链路、redirectTo 栈行为、诊断、GATT 读/写/监听、OTA 成功 | 13/13 PASS |
| 第二轮 | 2026-09-02 | 扫描/筛选/广播弹窗/断开/广播状态/溢出阻断/Web 不支持/错误恢复 | 17/17 PASS |
| 第二轮尾部 | 2026-09-02 | 关于/版本/P003 缺失态/扫描错误横幅/P006 重试/OTA hash 失败 | 9/9 PASS |
| 移除变更回归 | 2026-09-02 | 面板移除后 11 项回归 + 1 项误报（`<script>` 注释文本误匹配，可见文本复核为净） | 11 PASS + 1 误报复核通过 |
| **当日复核（本报告）** | 2026-09-02 | 见下 | 6/6 PASS |

当日复核明细（对当前磁盘文件重新加载）：
1. 加载零 JS 错误（console error = 0；仅首次访问 favicon 404，无害）；
2. P001 初始渲染含 SHID 卡与 5 类动作（toggle-scan/toggle-filter/open-adv/connect-generic/open-profile），**无「已配置 Smart HID」面板与「全部历史」残留**（仅存于代码注释中的变更说明）；
3. P001→P002 配网向导（三步骤条 + 连接确认文案）→ 返回键回 P001，栈行为正确；
4. tabBar→P009：版本 1.0.5 与推广卡渲染正常；
5. P008：31 字节预算呈现、返回键存在；
6. 评审面板：9 页直达 + 场景切换可用（当日实测 P009 场景数 1，与注册表一致）。

## 7. 缺失列表

**功能/页面/状态缺失：无。** 以下为非阻断发现项：

| 编号 | 级别 | 描述 | 建议 |
|---|---|---|---|
| D1 | 中（原型模拟不一致） | **P006 无页内返回入口**：其余子页（P002/P003/P005/P008/P010）均有自绘返回键，唯 renderP006 未渲染 subnav；真实 App 中 PAGE006 为 navigateTo 页、由系统导航栏提供返回。page-spec P006 按钮清单未列「返回」（隐含系统行为），故属**原型对系统返回的模拟缺失**而非规格缺失。影响：进入 P006 后只能靠评审面板/tabBar 离开。 | 修复约 2 行（renderP006 头部补 `subnav(d.name)` 即可），修复后本项复核归档 |
| O1 | 低（观感） | P008 为 tabBar 页但渲染了 subnav 返回键（栈长 1 时为无操作）；真实 tab 页无返回键 | 可顺手处理（P008 头部去返回键保留标题），不阻断 |
| O2 | 无害 | favicon 404（1 条网络错误，非 JS 错误） | 不处理 |

另说明（非缺失）：`REVERSE_ANALYSIS.md` 仍记载 PAGE004/F023/known_devices 存储，系**有意保留的历史记录**（逆向分析不回写），以 PRD.md 变更记录为最新口径。

## 8. 结论

**有条件通过。**

- 页面覆盖 10/10（在册口径）、功能覆盖 29/29、操作覆盖通过、状态覆盖 5/5、异常覆盖通过——与 COVERAGE_CHECKLIST 宣称的 100% 覆盖一致，未发现脑补功能或红线违规（未删复杂功能、未藏按钮、未简化流程、未新增商业功能）。
- 唯一建议修复项为 D1（P006 返回栏，2 行改动）；O1 可选。修复后仅需针对 D1 复核，无需全量回归。
- V0 作为「旧产品镜像」验收成立，可进入新 SOP Phase 4（产品体验审查 → `docs/06_review/PRODUCT_REVIEW.md`（v2.0 SOP））。

---

## 附注：D1/O1 处置 + 目录迁移至 SOP v2.0（2026-09-02 同日）

1. **D1 已修复并复核**：renderP006 头部补自绘返回栏（`subnav(设备名)`）。浏览器复核：P001→连接→P006 显示返回键（标题=设备名），点击返回 P001，栈行为正确。
2. **O1 已修复并复核**：P008（tab 页）移除返回键、保留标题栏「BLE 广播」，31 字节预算正常呈现。
3. **回归抽查通过**：P002 经正规入口（P001 SHID 卡「配置」）三步骤条与连接确认文案正常；评审面板直达 P002 在无设备上下文时显示守卫态「缺少设备上下文请从扫描页 SHID 卡进入」（与真实 App 需携带 deviceId 进入的约束一致，非缺陷）。控制台仅 favicon 404，无 JS 错误。
4. **验收结论由「有条件通过」升级为「通过」**——缺失列表清零（O2 favicon 404 维持不处理）。
5. **目录迁移**：按用户提供的《旧 App AI 重构 SOP v2.0》编号体系迁移（本报告路径引用已同步）：`docs/reverse-analysis.md → 01_reverse/REVERSE_ANALYSIS.md`、`docs/product/prd.md → 02_product/PRD.md`、`docs/product/page-spec.md → 03_flow/PAGE_SPEC.md`、`prototype/coverage-checklist.md → 09_test/COVERAGE_CHECKLIST.md`、`prototype/app-prototype.html → prototype/v0-old/app-prototype.html`。索引见 `docs/README.md`。
