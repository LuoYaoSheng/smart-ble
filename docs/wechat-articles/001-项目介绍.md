# 一款真正开源的跨平台 BLE 调试工具：8+ 种实现 + 硬件固件，全部开放！

---

> 市面上的蓝牙调试工具不少，但**完全开源**的寥寥无几。
>
> 能跨平台的也有，但大多只是**概念验证**，代码不完整。
>
> 能调试的很多，但**带硬件固件**的几乎没有。

今天给大家介绍一个不一样项目——**Smart BLE**。

---

## 三大核心卖点

### 🔓 完全开源

很多号称"开源"的蓝牙工具，核心代码往往闭源，或者只是一个简化版。

**Smart BLE 不同**：

| 开源内容 | 说明 |
|---------|------|
| ✅ 移动端代码 | uni-app、Flutter、原生 Android、原生 iOS |
| ✅ 桌面端代码 | Electron、Tauri、macOS 原生、Avalonia |
| ✅ 硬件固件 | ESP32 完整固件，可直接烧录使用 |
| ✅ 协议设计 | BLE 服务、数据格式、交互协议全部公开 |
| ✅ MIT 协议 | 商用无忧，可自由修改和分发 |

**没有黑盒，没有隐藏，代码即文档。**

---

### 🔌 8+ 种实现，覆盖所有主流平台

这不是一个"能跑就行"的 Demo，而是**生产级别的完整实现**：

#### 平台一：uni-app 版本 ✅

```
apps/uniapp/
├── pages/           # 页面
├── components/      # 组件
├── utils/           # BLE 封装
└── manifest.json    # 配置
```

- **技术栈**：Vue 3 + uni-ui
- **支持平台**：微信小程序、Android App、iOS App、H5
- **代码行数**：2000+
- **开发状态**：✅ 已完成，可直接使用

#### 平台二：Flutter 版本 ✅

```
apps/flutter/
├── lib/
│   ├── core/           # BLE 抽象层
│   ├── providers/      # Riverpod 状态管理
│   ├── screens/        # 页面
│   └── models/         # 数据模型
└── pubspec.yaml
```

- **技术栈**：Flutter 3.0+ + flutter_blue_plus + Riverpod
- **支持平台**：Android、iOS、macOS
- **特色功能**：支持 BLE 外设模式（手机变身蓝牙设备）
- **开发状态**：✅ 已完成，可直接使用

#### 平台三：原生 Android 🚧

```
apps/android/
├── app/src/main/
│   ├── java/.../ble/   # BLE 封装
│   └── java/.../ui/    # Jetpack Compose UI
└── build.gradle
```

- **技术栈**：Kotlin + Jetpack Compose
- **开发状态**：🚧 开发中

#### 平台四：原生 iOS/macOS 🚧

```
apps/ios/
├── SmartBLE/
│   ├── Core/           # CoreBluetooth 封装
│   └── Views/          # SwiftUI
└── SmartBLE-macos/     # macOS 版本
```

- **技术栈**：Swift + SwiftUI + CoreBluetooth
- **开发状态**：🚧 开发中

#### 平台五：桌面端全家桶 🎁

桌面端实现最为丰富，满足不同开发者需求：

##### Electron 版本 ✅

```
apps/desktop/electron/
├── src/                # JavaScript 源代码
├── public/             # 前端资源
└── package.json
```

- **技术栈**：JavaScript/Node.js + noble
- **支持平台**：Windows/macOS/Linux
- **特色**：功能最完整，Linux 支持广播模式

##### Tauri 版本 ✅

```
apps/desktop/tauri/
├── src-tauri/
│   ├── src/
│   │   └── main.rs     # Rust + btleplug
│   └── Cargo.toml
└── src/
    └── App.vue         # 前端
```

- **技术栈**：Rust + Tauri 1.5 + btleplug
- **支持平台**：Windows/macOS/Linux
- **特色**：安装包仅 ~10MB，内存占用低

##### macOS 原生版本 ✅

```
apps/desktop/macos/SmartBLE-mac/
├── Sources/            # Swift 源代码
├── Package.swift
└── Info.plist
```

- **技术栈**：Swift 5.9 + AppKit + CoreBluetooth
- **支持平台**：macOS 13+
- **特色**：原生 macOS 体验，支持广播模式

##### Avalonia (.NET) 版本 ✅

```
apps/desktop/avalonia/SmartBLE.Desktop/
├── Models/             # 数据模型
├── ViewModels/         # MVVM 模式
├── Views/              # XAML 界面
└── SmartBLE.Desktop.csproj
```

- **技术栈**：.NET 8 + Avalonia UI 11 + C#
- **支持平台**：Windows 优先
- **特色**：.NET 生态，Windows 用户首选

---

### 🤖 硬件固件，端到端测试

软件调试工具有很多，但**带硬件固件**的很少。

Smart BLE 项目内置 **ESP32 完整固件**：

```
hardware/esp32/
├── main/
│   ├── ble_server.cpp      # BLE 服务实现
│   ├── led_control.cpp     # LED 控制
│   └── command_handler.cpp # 命令处理
├── sdkconfig               # 配置文件
└── CMakeLists.txt
```

#### 固件功能

| 功能 | 说明 |
|------|------|
| 🔤 自定义设备名称 | 可配置广播名称 |
| 📡 LED 控制 | 常亮 / 快闪 / 慢闪 |
| 📄 JSON 数据交互 | 标准化数据格式 |
| 🔒 多权限特征值 | Read / Write / Notify |

#### 实测硬件支持

- ✅ ESP32 系列全兼容
- ✅ ESP32-C3（已测试）
- ✅ ESP32-S2/S3（理论上兼容）
- 📝 nRF52、STM32 移植指南（文档中）

#### 端到端测试流程

```
手机 App ──BLE 扫描──> 发现 ESP32 设备
    │
    ├── 连接 ──> 发现服务
    │
    ├── 写入 ──> {"cmd":"led","mode":"fast"}
    │
    └── 订阅 ──> 接收设备状态通知
```

**软件 + 硬件，一站式解决方案。**

---

## 为什么做这个项目？

### 起源

作为蓝牙开发者，我们深深体会到：

1. **工具割裂**：每个平台都要用不同的工具，体验不一致
2. **代码分散**：网上代码片段很多，但完整的跨平台实现很少
3. **硬件脱节**：软件工具和硬件开发往往分离，缺乏端到端方案
4. **学习成本**：新人入门蓝牙开发，门槛太高

### 目标

Smart BLE 的目标很明确：

- ✅ **实用工具**：日常蓝牙开发调试神器
- ✅ **学习资源**：跨平台 BLE 开发最佳实践
- ✅ **开源贡献**：回馈社区，让蓝牙开发更简单

---

## 核心功能一览

### 📱 设备端（Central 模式）

| 功能 | 描述 |
|------|------|
| 扫描设备 | 信号强度过滤、名称过滤、节流处理 |
| 连接管理 | 一键连接、自动发现服务 |
| 数据读写 | UTF-8 / HEX 双格式支持 |
| 通知订阅 | 实时接收设备数据 |
| 操作日志 | 完整记录每一步操作 |

### 📡 外设端（Peripheral 模式）

| 功能 | 描述 |
|------|------|
| BLE 广播 | 自定义名称、服务 UUID、厂商数据 |
| 模拟设备 | 手机可变身蓝牙设备，用于测试 |
| 跨平台 | Android / iOS / macOS 全支持 |

---

## 代码质量保证

### 统一抽象层

所有平台共享相同的 BLE 抽象接口：

```typescript
interface IBLEAdapter {
    // 初始化
    initialize(): Promise<void>

    // 扫描
    startScan(options?: ScanOptions): Promise<void>
    stopScan(): Promise<void>

    // 连接
    connect(deviceId: string): Promise<void>
    disconnect(): Promise<void>

    // 服务发现
    discoverServices(deviceId: string): Promise<Service[]>

    // 读写操作
    readCharacteristic(serviceId: string, charId: string): Promise<DataBuffer>
    writeCharacteristic(serviceId: string, charId: string, data: DataBuffer): Promise<void>

    // 通知
    setNotification(serviceId: string, charId: string, enable: boolean): Promise<void>

    // 外设模式
    startAdvertising(options: AdvertisingOptions): Promise<void>
    stopAdvertising(): Promise<void>
}
```

**学习一个平台，其他平台触类旁通。**

### 完整文档

```
docs/
├── 01-functional-specs.md      # 功能规格文档
├── 02-data-flow.md             # 数据流图
├── 03-ble-protocol.md          # BLE 协议定义
├── 04-ui-flows.md              # UI 流程文档
└── 05-platform-differences.md  # 平台差异说明
```

---

## 快速开始

### 方式一：直接使用（uni-app 版本）

```bash
# 克隆项目
git clone https://github.com/yourusername/smart-ble.git
cd smart-ble/apps/uniapp

# 安装依赖
npm install

# 运行到微信小程序
npm run dev:mp-weixin

# 打开微信开发者工具，导入 dist/dev/mp-weixin 目录
```

### 方式二：Flutter 版本

```bash
cd smart-ble/apps/flutter
flutter pub get
flutter run
```

### 方式三：烧录 ESP32 固件

```bash
cd smart-ble/hardware/esp32
idf.py build
idf.py -p /dev/ttyUSB0 flash monitor
```

---

## 谁适合使用？

| 用户类型 | 推荐使用方式 |
|---------|-------------|
| 蓝牙设备开发者 | 直接使用 App 作为日常调试工具 |
| 跨平台开发者 | 参考代码，移植到自己的项目 |
| 嵌入式工程师 | 使用 ESP32 固件作为开发模板 |
| 蓝牙学习者 | 阅读代码 + 烧录硬件，实践学习 |
| 企业用户 | 基于 MIT 协议进行二次开发 |

---

## 开源协议

**MIT License**

- ✅ 商业使用
- ✅ 修改和分发
- ✅ 专利使用
- ✅ 私人使用

**没有任何限制，完全自由。**

---

## 项目地址

```
GitHub: https://github.com/luoyaosheng/smart-ble
Gitee:  https://gitee.com/luoyaosheng/smart-ble/tree/refactor%2Fmulti-platform/
```

---

## 写在最后

Smart BLE 不是为了重复造轮子，而是：

1. **填补空白**：真正开源的跨平台 BLE 工具
2. **降低门槛**：让蓝牙开发更易上手
3. **服务社区**：分享我们的实践和经验

如果你觉得这个项目有帮助：

- ⭐ 给个 Star，让更多人看到
- 🍴 Fork 代码，提交你的 PR
- 📢 分享给身边的开发者
- 💬 提 Issue，帮助我们改进

---

**让蓝牙开发，从此简单！**

---

## 🔜 系列预告

这是 **Smart BLE 系列文章**的第一期。接下来我们将持续分享：

| 期数 | 标题 | 预计发布 |
|------|------|---------|
| #1 | 项目介绍（本文） | ✅ 已发布 |
| #2 | 开发路线图：我们要做什么？ | 敬请期待 |
| #3 | Bug 修复记录 | 按需发布 |
| #4 | 新功能解析 | 版本更新时 |
| #5 | 技术深度解析 | 每月一期 |
| #6 | 平台对比与选型 | 待定 |
| #7 | 硬件实战：ESP32 从零搭建 | 待定 |

**关注公众号，第一时间获取系列文章更新！**

---

*文末扫码关注公众号，回复「BLE」获取项目链接和技术交流群*

*本文为原创技术文章，转载请注明出处*
